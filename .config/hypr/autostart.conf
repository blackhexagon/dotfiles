# Extra autostart processes
# exec-once = uwsm-app -- my-service

# Pomodoro timer (uair). Clean stale socket from any unclean exit, then start.
exec-once = uwsm-app -- sh -c 'rm -f "$XDG_RUNTIME_DIR/uair.sock"; exec uair'
